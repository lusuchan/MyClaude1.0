[pytest]
testpaths = tests
pythonpath = . tests
addopts = -m "not live"
markers =
    live: hits the real network and the real Claude API — opt in with `-m live`
filterwarnings =
    ignore::DeprecationWarning
